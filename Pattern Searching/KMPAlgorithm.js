/*
 * Given a string we will KMP algorithm to optimize Naive implementation.
 * The worst case time complexity is O(n) 
 * But to calculate the prefix - time complexity is O(MM)
 */

var inputArray = "AABBBAAAABABA";
var pattern = "ABABBA";

// calculating prefix/lps table
var prefix = getPrefix();

var getPrefix = function(){
    for(var i=0;i<pattern.length;i++){

    }
}